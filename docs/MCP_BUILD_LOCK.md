# ALTAREEQ Cloudflare MCP

This branch is reserved for the project-scoped Cloudflare MCP gateway. The server uses Streamable HTTP at `/mcp`, OAuth 2.1 for MCP authorization, a GitHub allowlist for human identity, and a server-side Cloudflare API token with least privilege.
