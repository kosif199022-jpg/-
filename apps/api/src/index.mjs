import {runtimeConfig,authenticate} from './auth.mjs';import {withClient,provisionedActor,assertOrgAccess} from './db.mjs';import {postJournal} from './posting.mjs';import {validateJournal} from '../../../packages/accounting/src/journal.mjs';
function json(body,status=200,ctx={requestId:'',correlationId:''}){return new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json;charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff','x-frame-options':'DENY','referrer-policy':'no-referrer','x-request-id':ctx.requestId,'x-correlation-id':ctx.correlationId}})}function errorBody(code){return{error:{code}}}function sameOrigin(request){const site=request.headers.get('sec-fetch-site');if(site&&!['same-origin','same-site','none'].includes(site))return false;const origin=request.headers.get('origin'),referer=request.headers.get('referer');if(!origin&&!referer)return false;try{const expected=new URL(request.url).origin;if(origin&&new URL(origin).origin!==expected)return false;if(!origin&&referer&&new URL(referer).origin!==expected)return false;return true}catch{return false}}
export default{async fetch(request,env){const requestId=crypto.randomUUID(),correlationId=request.headers.get('x-correlation-id')||crypto.randomUUID(),ctx={requestId,correlationId},url=new URL(request.url);try{if(url.pathname==='/api/health')return json({status:'ok',service:'altareeq'},200,ctx);if(url.pathname==='/api/capabilities'&&request.method==='GET')return env.ASSETS.fetch(new Request(new URL('/capabilities.json',url),request));if(url.pathname==='/api/accounting/validate-journal'&&request.method==='POST'){if(!sameOrigin(request))return json(errorBody('ORIGIN_NOT_ALLOWED'),403,ctx);const body=await request.json(),r=validateJournal(body);return json({valid:r.valid,debitMinor:r.debitMinor.toString(),creditMinor:r.creditMinor.toString(),errors:r.errors},r.valid?200:422,ctx)}if(url.pathname.match(/^\/api\/accounting\/journals\/[0-9a-f-]+\/post$/)&&request.method==='POST'){if(!sameOrigin(request))return json(errorBody('ORIGIN_NOT_ALLOWED'),403,ctx);const config=runtimeConfig(env),actor=await authenticate(request,config),body=await request.json(),journalId=url.pathname.split('/')[4],organizationId=String(body.organizationId||''),idempotencyKey=String(request.headers.get('idempotency-key')||'').trim();if(!organizationId||!idempotencyKey||idempotencyKey.length>128)return json(errorBody('VALIDATION_FAILED'),400,ctx);const result=await withClient(env,async client=>{const actorId=await provisionedActor(client,actor.subject);await assertOrgAccess(client,actorId,organizationId);return postJournal(client,{organizationId,journalId,actorId,idempotencyKey,requestId,correlationId})});return json(result,200,ctx)}if(url.pathname.startsWith('/api/'))return json(errorBody('API_ROUTE_NOT_FOUND'),404,ctx);return env.ASSETS.fetch(request)}catch(e){const code=e instanceof Error?e.message:'INTERNAL_ERROR',status=Number(e?.status)||500;return json(errorBody(code),status,ctx)}}};
