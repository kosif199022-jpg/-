# ChatGPT client setup

After live deployment, register the final HTTPS `/mcp` endpoint as a custom MCP app in ChatGPT Developer Mode and select OAuth. Do not register a localhost URL or the placeholder `invalid.example` origin.
