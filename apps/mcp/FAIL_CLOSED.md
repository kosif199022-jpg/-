# Fail closed

Missing KV ids, public origin, OAuth credentials, cookie-signing material, Cloudflare API token, account context, or required MCP scopes must block the affected operation rather than fall back to anonymous or permissive behavior.
