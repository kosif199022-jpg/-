# No generic proxy

The ALTAREEQ MCP server does not accept arbitrary Cloudflare API paths, arbitrary origins, arbitrary HTTP methods, or arbitrary request bodies. Each exposed capability is a named tool with an explicit input schema and server-side scope check.
