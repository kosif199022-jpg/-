# Live test required

After account provisioning, run a live OAuth login and invoke every read tool against the authorized Cloudflare account before declaring the MCP connection production-ready. Exercise the write scope separately and revoke it when provisioning is not required.
