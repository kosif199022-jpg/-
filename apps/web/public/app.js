const root=document.documentElement,nav=document.querySelector('#nav'),content=document.querySelector('#content'),metrics=document.querySelector('#metrics'),apiStatus=document.querySelector('#apiStatus');
const moduleLabels={overview:'الرئيسية',accounting:'المحاسبة العامة',ar:'العملاء وAR',ap:'الموردون وAP',reconciliation:'المطابقات',procurement:'المشتريات',treasury:'الخزينة',assets:'الأصول الثابتة',inventory:'المخزون',close:'الإقفال',budgeting:'الموازنة',intercompany:'بين الشركات',revenue:'العقود والإيراد',lease:'الإيجارات',audit:'المراجعة',reporting:'التقارير',analytics:'التحليلات',tax:'الضريبة والزكاة',import_export:'الاستيراد',knowledge_brain:'عقل المعرفة',ai:'الذكاء الاصطناعي',workflow:'سير العمل',library_reader:'المكتبة والقارئ',audio_media:'الصوت',sales:'المبيعات',security:'الأمان',operations:'التشغيل',requirements:'التكافؤ',design_ux:'التصميم',pwa_offline:'PWA',developer_quality:'الجودة',labs:'المختبر'};let modules=[['overview',moduleLabels.overview]],capabilities=[],current='overview';
function labelStatus(s){return({parity_verified:'متحقق',in_progress:'قيد التنفيذ',not_started:'لم يبدأ',deprecated:'متقاعد',rejected:'مرفوض'})[s]||s}
function syncModules(){const seen=new Set(modules.map(x=>x[0]));for(const id of [...new Set(capabilities.map(x=>x.module))].sort())if(!seen.has(id))modules.push([id,moduleLabels[id]||id])}
function renderNav(){nav.innerHTML='';for(const [id,label] of modules){const count=capabilities.filter(x=>x.module===id).length,b=document.createElement('button');b.type='button';b.dataset.module=id;b.setAttribute('aria-current',current===id?'page':'false');b.innerHTML=`<span>${label}</span><small>${count||''}</small>`;b.addEventListener('click',()=>{current=id;renderNav();render();document.querySelector('#main').focus({preventScroll:true})});nav.appendChild(b)}}
function renderMetrics(){const total=capabilities.length,verified=capabilities.filter(x=>x.parity_status==='parity_verified').length,progress=capabilities.filter(x=>x.parity_status==='in_progress').length,critical=capabilities.filter(x=>x.risk_class==='critical').length;metrics.innerHTML=[[total,'قدرة محفوظة'],[verified,'متحقق آليًا'],[progress,'قيد التنفيذ'],[critical,'حرجة الحوكمة']].map(x=>`<div class="metric"><b>${x[0]}</b><span>${x[1]}</span></div>`).join('')}
function overview(){const professional=[['النواة المالية','Money + Journal + immutable posting + periods + GL'],['ERP التشغيلي','AR/AP/Sales/Purchases/Treasury/Assets/Inventory/Close'],['المراجعة','8 مراحل + مخاطر + PBC + أدلة + إجراءات + نتائج + تسويات + تقرير'],['عقل المعرفة','مصادر وإصدارات وصفحات وكتل واستشهادات واسترجاع هجين'],['AI Council','استشاري متعدد النماذج مع قرار بشري'],['القارئ والصوت','كتب ومعايير وTTS/Offline/PWA وخبرة iPhone'],['الهندسة والجودة','Parity + CI + Security + WebKit + Accessibility + Production Smoke']];return `<div class="section-head"><div><h2>خريطة النظام</h2><p>لا تعرض أرقامًا مالية تجريبية كأنها بيانات إنتاج.</p></div></div><div class="domain-list">${professional.map(x=>`<article class="domain"><h3>${x[0]}</h3><p>${x[1]}</p></article>`).join('')}</div>`}
function moduleView(id){const rows=capabilities.filter(x=>x.module===id);if(!rows.length)return `<div class="empty">لا توجد قدرات مسجلة في هذا المجال.</div>`;return `<div class="section-head"><div><h2>${modules.find(x=>x[0]===id)?.[1]||id}</h2><p>الحالة هنا حالة تنفيذ ALTAREEQ وليست ادعاءً بنضج المصدر القديم.</p></div></div><div class="filterbar"><input id="q" aria-label="بحث" placeholder="ابحث في القدرات"><select id="risk"><option value="">كل المخاطر</option><option value="critical">حرج</option><option value="high">عالٍ</option><option value="normal">عادي</option><option value="low">منخفض</option></select></div><div class="table-wrap"><table><thead><tr><th>القدرة</th><th>الوصف</th><th>المخاطر</th><th>الحالة</th></tr></thead><tbody id="rows"></tbody></table></div>`}
function fillRows(id){const body=document.querySelector('#rows');if(!body)return;const q=(document.querySelector('#q')?.value||'').trim().toLowerCase(),risk=document.querySelector('#risk')?.value||'';const rows=capabilities.filter(x=>x.module===id&&(!risk||x.risk_class===risk)&&(!q||`${x.id} ${x.name_ar} ${x.description_ar}`.toLowerCase().includes(q)));body.innerHTML=rows.map(x=>`<tr><td><strong>${x.name_ar}</strong><div class="code">${x.id}</div></td><td>${x.description_ar}</td><td><span class="risk ${x.risk_class}">${x.risk_class}</span></td><td><span class="state ${x.parity_status}">${labelStatus(x.parity_status)}</span></td></tr>`).join('')||'<tr><td colspan="4">لا نتائج.</td></tr>'}
function render(){content.innerHTML=current==='overview'?overview():moduleView(current);if(current!=='overview'){fillRows(current);document.querySelector('#q').addEventListener('input',()=>fillRows(current));document.querySelector('#risk').addEventListener('change',()=>fillRows(current))}}
document.querySelector('#themeBtn').addEventListener('click',()=>{root.dataset.theme=root.dataset.theme==='dark'?'light':'dark'});
try{const r=await fetch('/api/capabilities',{cache:'no-store'});capabilities=await r.json()}catch{try{capabilities=await fetch('/capabilities.json').then(r=>r.json())}catch{capabilities=[]}}
try{const h=await fetch('/api/health',{cache:'no-store'});apiStatus.textContent=h.ok?'الخدمة تعمل':'الخدمة غير جاهزة'}catch{apiStatus.textContent='واجهة مستقلة — API غير متصل'}
syncModules();renderMetrics();renderNav();render();if('serviceWorker'in navigator&&location.protocol==='https:')navigator.serviceWorker.register('/sw.js').catch(()=>{});
