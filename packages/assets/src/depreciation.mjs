import {parseMoney} from '../../accounting/src/money.mjs';
export function straightLineSchedule({cost,residual='0',usefulLifeMonths,scale=2}){const costMinor=parseMoney(cost,scale),residualMinor=parseMoney(residual,scale);if(costMinor<0n||residualMinor<0n||residualMinor>costMinor)throw new Error('ASSET_VALUE_INVALID');if(!Number.isInteger(usefulLifeMonths)||usefulLifeMonths<1||usefulLifeMonths>1200)throw new Error('USEFUL_LIFE_INVALID');const depreciable=costMinor-residualMinor,months=BigInt(usefulLifeMonths),base=depreciable/months,remainder=depreciable%months,out=[];let accumulated=0n;for(let i=0;i<usefulLifeMonths;i++){const amount=base+(BigInt(i)<remainder?1n:0n);accumulated+=amount;out.push(Object.freeze({period:i+1,depreciationMinor:amount,accumulatedMinor:accumulated,netBookValueMinor:costMinor-accumulated}))}if(out.at(-1)?.netBookValueMinor!==residualMinor)throw new Error('DEPRECIATION_INVARIANT_FAILED');return Object.freeze(out)}
