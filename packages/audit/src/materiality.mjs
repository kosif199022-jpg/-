import {parseMoney} from '../../accounting/src/money.mjs';
function mulDivRoundHalfUp(value,numerator,denominator){if(denominator<=0n)throw new Error('INVALID_DENOMINATOR');const sign=value<0n?-1n:1n,n=value<0n?-value:value;return sign*((n*numerator+denominator/2n)/denominator)}
export function calculateMateriality({benchmarkAmount,benchmarkBps,performanceBps=7500,trivialBps=500,scale=2}){const base=parseMoney(benchmarkAmount,scale);if(base<=0n)throw new Error('MATERIALITY_BENCHMARK_INVALID');for(const x of [benchmarkBps,performanceBps,trivialBps])if(!Number.isInteger(x)||x<0||x>10000)throw new Error('MATERIALITY_RATE_INVALID');const overall=mulDivRoundHalfUp(base,BigInt(benchmarkBps),10000n),performance=mulDivRoundHalfUp(overall,BigInt(performanceBps),10000n),clearlyTrivial=mulDivRoundHalfUp(overall,BigInt(trivialBps),10000n);return Object.freeze({benchmarkMinor:base,overallMinor:overall,performanceMinor:performance,clearlyTrivialMinor:clearlyTrivial})}
