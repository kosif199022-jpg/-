import test from 'node:test'; import assert from 'node:assert/strict'; import { assertPostableJournal, reversalLines, canonicalPostingHash } from '../src/posting.mjs';
const journal={status:'approved',periodStatus:'open',lines:[{accountId:'a',debit:'10',credit:'0'},{accountId:'b',debit:'0',credit:'10'}]};
test('postability enforces approval period and balance',()=>{ assert.equal(assertPostableJournal(journal).valid,true); assert.throws(()=>assertPostableJournal({...journal,status:'draft'}),/JOURNAL_NOT_APPROVED/); assert.throws(()=>assertPostableJournal({...journal,periodStatus:'closed'}),/PERIOD_CLOSED/); });
test('reversal swaps debit and credit without mutation',()=>{ const out=reversalLines(journal.lines); assert.deepEqual(out[0],{accountId:'a',debit:'0',credit:'10'}); assert.equal(journal.lines[0].debit,'10'); });
test('posting hash is deterministic and chained',()=>{ const x=canonicalPostingHash({journalId:'j1',organizationId:'o1',lines:[{accountId:'a',debitMinor:100n,creditMinor:0n}]}); const y=canonicalPostingHash({journalId:'j1',organizationId:'o1',lines:[{accountId:'a',debitMinor:100n,creditMinor:0n}]}); assert.equal(x,y); assert.equal(x.length,64); });
