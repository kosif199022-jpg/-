export function validateCitation(c){const errors=[];if(!c?.sourceVersionId)errors.push('SOURCE_VERSION_REQUIRED');if(!c?.blockId)errors.push('SOURCE_BLOCK_REQUIRED');if(!Number.isInteger(c?.page)||c.page<1)errors.push('SOURCE_PAGE_REQUIRED');if(!c?.checksum||!/^[0-9a-f]{64}$/i.test(c.checksum))errors.push('SOURCE_CHECKSUM_REQUIRED');return Object.freeze({valid:errors.length===0,errors:Object.freeze(errors)})}
export function assertProfessionalAnswerCitations(citations){if(!Array.isArray(citations)||citations.length===0)throw new Error('CITATION_REQUIRED');for(const c of citations){const r=validateCitation(c);if(!r.valid)throw new Error(r.errors[0])}return true}
